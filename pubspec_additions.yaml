# Add these under `dependencies:` in your existing pubspec.yaml.
# Versions are pinned to recent stable releases â€” adjust if your
# project already locks different versions for these packages.

dependencies:
 provider: ^6.1.2
go_router: ^14.6.2
dio: ^5.7.0
uuid: ^4.5.1
intl: ^0.19.0

# Make sure the mock JSON assets are declared under `flutter: assets:`
flutter:
  assets:
    - assets/mock/addresses.json
    - assets/mock/wishlist.json
    - assets/mock/gst_details.json
    - assets/mock/gift_cards.json

